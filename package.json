{
  "name": "bsdemo",
  "version": "0.0.1",
  "description": "a browser-sync demo",
  "main": "index.html",
  "scripts": {
    "postinstall": "browser-sync init && mv bs-config.js bs-config.origin.js && cp bs-config.sample.js bs-config.js",
    "start": "browser-sync start --config bs-config.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Masaaki Komori",
  "license": "MIT",
  "devDependencies": {
    "browser-sync": "^1.3.2"
  }
}
